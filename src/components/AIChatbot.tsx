/**
 * ChatKit widget artık index.html'den yükleniyor (route değişimlerinde
 * partial render / CSP sorunu olmasın diye React dışında).
 * Bu component geriye uyumluluk için kaldı, render etmiyor.
 */
export default function AIChatbot() {
  return null;
}
